# Current Users and Passwords

Date: 2026-03-09

## Database status

- SQLite file at `backend/db.sqlite3` exists but is empty (`0 bytes`).
- PostgreSQL container (`db`) has no tables (`Did not find any relations`).

## Users found

No users were found in the current database state.

## Passwords

No passwords are available because there are no user records.

> Note: In Django, passwords are stored as hashes, not plain text.
